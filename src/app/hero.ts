const { heroui } = require('@heroui/theme');

// Tailwind v4 local plugin entry for HeroUI.
// Referenced by @plugin './hero' in global.css
module.exports = heroui({
  // You can customize themes or tokens here later if needed
  // See: https://heroui.dev/docs/theming
});